CAMBIOS DE ESTA VERSION (persistencia + cara + desequipar)

CAUSA RAIZ de "cuenta eliminada" / "sesion no valida" / "Perfil no encontrado":
1. .gitignore tenia "data/" entero → en GitHub NO subia session-secret.txt
   ni nada de data. Cada Manual Deploy regeneraba el secreto y el
   users.json quedaba vacio → tokens invalidos y cuentas "desaparecidas".
2. En plan Free de Render el disco del proyecto es EFIMERO. Sin Postgres
   las cuentas se pierden en cada redeploy.
3. sessionSecretFromEnv:false en /health = Render no tenia SESSION_SECRET.

ARREGLOS:
- .gitignore: ya NO ignora data/session-secret.txt ni data/admins.json
- server.js: SESSION_SECRET estable (nunca aleatorio entre deploys)
- server.js: /health muestra postgresReady y aviso si falta Postgres
- server.js: Desequipar funciona aunque el item no este en inventario
- server.js: no borra caras/items clasicos del equipamiento por filtro de inventario
- public/index.html: Classic Smile usa SVG (antes imagen rota en catalogo)

EN RENDER (OBLIGATORIO):
1. Environment → SESSION_SECRET = (cualquier cadena larga fija, NO la cambies nunca)
2. Environment → DATABASE_URL = la de tu Postgres free (epicbloxs-db)
3. Manual Deploy after push
4. Abre /health y verifica:
   - postgresReady: true
   - sessionSecretFromEnv: true